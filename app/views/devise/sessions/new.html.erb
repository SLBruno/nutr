<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<h2>Logar</h2>

		<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
		  <div class="form-inputs">
		    <%= f.input :email, required: false, autofocus: true, input_html: { class: 'form-control' } %>
		    <%= f.input :password, required: false, label: "Senha", input_html: { class: 'form-control' } %>
		    <%= f.input :remember_me, as: :boolean, label: "Manter logado?", input_html: { class: 'form-control' } if devise_mapping.rememberable? %>
		  </div>

		  <div class="form-actions">
		    <%= f.button :submit, "Logar", class: 'btn btn-primary' %>
		  </div>
		  <br>
		<% end %>

		<%= render "devise/shared/links" %>
	</div>
</div>
